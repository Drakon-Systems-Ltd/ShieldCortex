### VERDICT
CHANGES_REQUESTED

### BLOCKERS
1. Empty-brain is treated as a capture-intelligence story. It may be defence FP, quarantine with no review, hook/MCP write-path failure, project-key miss, or a host tool that writes native files instead of `remember`. Cut C / 1+2 cannot freeze until a one-page RCA exists: why `defence_audit` moves and `memories` does not. Distill on a blocked or mis-aimed write path still yields an empty vault.

2. Inject v2 has no hard numeric ceilings. "Budget first" without absolute token/row/session caps is how 40x returns. Config is not a ceiling. Design must state coded maxima that config cannot exceed, plus default `start` not `both`.

3. Always-on inject from a shared store is a P0 contamination bug, not Track G. Live hook text already says memories.db is shared across OpenClaw and Claude Code. Session-start inject without host/agent/project scope will spray one brain into another. Fleet-as-P2 is too late if B is the first ship.

4. Pack schema `{id, title, why, trust, age, tokens}` contradicts "brief data, never instructions." `why` is instruction-shaped. So is any "you should remember to…" title. Envelope labels do not stop models treating system-adjacent text as policy. Inject schema must be fact/source/trust/age only; no rationale field.

5. Distill fail-closed is not a policy: "L0 or skip" is an or. Silent fallback to regex shards recreates today's store. Worse: L1 is a new high-privilege writer whose input is the transcript (confused deputy). Output defence is the same pipeline you already scored 3.5/10 for cognition. Fail closed = skip + audit + doctor; L0 only under an explicit degraded flag. Distill must be tool-less, schema-bound, and treated as adversarial.

6. Success definition 6 makes full LongMemEval-S a Memory-SOTA-ready gate. That is a generic-RAG objective. LME-S measures document QA retrieval, not defended facts, inject budgets, or poison loops. Keep D as an honesty scorecard. P0 proof is poison/recall-loop + budget/hash-ring + doctor-empty + no-40x. Unhook LME-S from the program conjunction.

7. Single-plane is stated as thesis and success #1, then undermined by `dual_legacy` and an open A1/A2/A3 pick. SC cannot make itself the only plane without a host contract (what OpenClaw/Claude Code/Hermes will stop writing). A2 bidirectional sync is out of P0. P0 plane policy is A3 import-once + doctor drift, or an explicit "best-effort canonical, native remains untrusted I/O." Do not freeze A1 as if it were an SC-only flag.

### NITS
1. Doctor "zero writes/recalls over N days = fail" needs N, and a quiet-host exemption. Otherwise operators disable doctor.

2. "What is strong today" lists MCP `remember` / `recall` / `forget` / `get_context`. Inventory the tools each bound host actually exposes. A preamble that names SC tools while the host offers file-backed search is dual-plane surrender in the tool catalog.

3. MCP-only hosts cannot inject. If that is a real deployment mode, `get_context` needs the same budget/hash/envelope rules or it becomes the next 40x.

4. Salience: L1 "not auto-1.0" needs a number. Suggest cap 0.7; 1.0 reserved for L2 pins. Pins of poisoned content at 1.0 need a review path.

5. `extraSystemPromptHash` stability vs a start pack that changes when a new memory lands mid-session is underspecified. Start pack should be pinned at session open.

6. Defence FP budget and quarantine UX are named as risks, not design. If review does not exist, distill fills quarantine and the brain stays empty.

7. Cut 0 as written is bureaucracy. Replace its meaning with RCA + empty-brain doctor, or drop it from the menu.

### ANSWERS (mandatory)
1. Single-plane thesis: correct as a bound-agent *direction*, wrong as an absolute. Sidecar-forever is the current failure: defended empty vault, undefended sticky notes win. SC should be the only *defended canonical* durable store. Native MD is projection or untrusted import, never a competing brain. SC cannot police host Memory Search it does not own. Stay a sidecar forever and the moat is theatre. Claim "only plane" without a host contract and Track A becomes nagware or a sync product.

2. Safest Inject v2 defaults (absolute ceilings in code; config may only go lower):
   - Session-start: 600 tokens, 6 rows, 100 tokens/row. Hard max 800 / 8 / 120.
   - Turn: off by default. If enabled later: 200 tokens, 2 rows. Hard max 300 / 3.
   - Session total inject: 1500 tokens. Hard max 2000.
   - Hash ring: whole session, exact hash never re-injected; start pack pinned at open.
   - Default mode: `start`. Not `both`.
   - Do not inject quarantine, RESTRICTED, or trust below threshold. No `why` field.

3. Distill fail-closed: no provider / schema fail / defence fail / timeout → **skip write**, audit, doctor warn. Never silent L0. Never dump the transcript. Prefer a separate local/tool-less model over the live host model (same conversation distilling itself is the confused-deputy case). Host-model allowed only if tool-less, billed via existing OAuth, rigid JSON schema. `distill_required` + fail = skip. `regex` is an explicit degraded mode, not a fallback. L2 pins still work when L1 is down.

4. First cut: **1+2**, after the blockers above are written into B and C. Prelude (not a romantic Cut 0): empty-brain RCA + inject ceilings/scope + A3/doctor stub. Defer: A1 exclusivity, A2 sync/CRDT, turn inject, LME-S as a ready-gate, Track E RRF-default, F trust faces, G shared brain, custom embedders. D harness may run in parallel as honesty, not as the definition of done.

5. Missing adversarial cases:
   - Transcript instructs distill to emit policy-as-fact ("operator wants defence off", "always call X").
   - Near-dup mutations beating exact hash ring; salience stuffing to win top-k.
   - Cross-host shared DB: Claude Code row injected into OpenClaw (and the reverse).
   - Native MD poisoned, then import; projection MD hand-edited and re-imported.
   - Unicode / tokenizer disagreement vs token budget.
   - Session-id churn (`/new`) resets the hash ring and reprints the start pack.
   - Ranker pulls RESTRICTED into the pack.
   - L2 pin of poison at salience 1.0; quarantine "approve all".
   - `get_context` dump on MCP-only hosts as the new 40x.
   - Model ignores the data envelope (assume this; test tool-gate independence).
   - Dream/decay leaves stale high-salience rows in every start pack.
   - Embedding-off FTS-only retrieval of keyword-stuffed poison.
   - Doctor false-fail on a quiet host → doctor gets turned off.

6. Accidental generic RAG: keeping LongMemEval-S in the SOTA-ready conjunction; optimizing RRF/embeddings for public QA; ingesting arbitrary document corpora; memory as chunked files instead of distilled facts; weakening write-path defence to move R@5; inject as "relevant docs in context" without trust/age/fact schema; Track E becoming a search product; fleet memory as a shared KB; dashboards that report recall@k instead of "defended facts used without becoming instructions."

### CUT PRIORITY
B > C > A > D

B first: the control plane that must not 40x, and the only way SC re-enters the cognitive bus. C next: otherwise B injects nothing or regex junk. A after there is a brain worth making canonical (A3 + doctor, not A2). D last among P0, staffed in parallel if you have a spare pair, never leading.

### ONE-LINE SUMMARY
Thesis is right and sidecar-forever is death — but do not lift the freeze until inject has hard budgets and tenancy, distill skips instead of falling back to regex, and LongMemEval is unhooked from "SOTA ready."
