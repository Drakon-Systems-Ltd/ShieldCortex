VERDICT
APPROVE_WITH_NITS

BLOCKERS
none

NITS
1. Host modes are now a closed P0 set but still unnamed in operations. One sentence each: `disable_native_inject` = host native recall/inject off, SC start-pack is the only automatic memory on the bus; `sc_only` = same bus rule plus native MD treated as non-brain (archive/view). Illegal: `memory.inject != off` with no recorded per-host choice. `coexist_dedup` stays out.

2. Pin-set + hash ring are adapter-side session state, keyed by host session id, never stored only in the prompt. Compact that cannot signal stays no-rehydrate, not a silent reprint.

3. Rehydrate the pinned snapshot (ids + hashes + frozen title/fact/age/tokens), not a live reserialize. Drop any row that fails live eligibility. `extraSystemPromptHash` then changes only when the eligible set shrinks — not because age ticked.

4. Write path must stamp `host_id` / `agent_id` / `project` (documented default sentinel, never NULL). Unscoped-only stores are doctor-fail green-wash, same as quarantine-only. Publish N, new-host grace, and fail-reason taxonomy (empty vs unscoped vs junk vs `inject=off`).

5. After C: unset + no provider = skip + audit + doctor, not regex. `regex` only when explicitly configured.

6. Title+fact are untrusted data inside the envelope; cap title so it cannot eat the per-row 100.

7. Editorial before first B PR (already assigned): SUPERSEDE parent `why`, open A2 / open cut-menu, `dual_legacy` as a product mode, session-clear hatch, and §8 LME-as-merge-gate. Hermes = MCP-only limitation, not implied inject parity.

FIRST CUT
Confirm: prelude empty-brain RCA (no feature code) → B start-only inject v2 (ceilings, out-of-prompt ring, compact rehydrate, host contract) → C fail-closed distill; A-min parallel with B; D harness parallel non-gating.

B must not default-on for a host until that host’s native-inject choice is recorded (already R3 law, not a different cut). If RCA shows write-path death, intake fix before C; B may still ship on fixtures.

Defer stays: A2, turn inject, Full P0, A1 projections until B+C prove value, A-min+ full import, fleet shared brain, RRF-as-product-gate, custom embedders, generic doc connectors.

ONE-LINE SUMMARY
R3 closed the freeze-lifters (one rehydrate law, live eligibility, no coexist_dedup); thesis and cut are right — first B PR hygiene only, no more thesis rounds.
