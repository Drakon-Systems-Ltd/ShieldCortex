### VERDICT
APPROVE_WITH_NITS

### BLOCKERS
none

### NITS
1. Parent §0–§15 still greppable with superseded pack `why`, open A2, and full cut-menu — Round-3 already schedules SUPERSEDED scrub before first B PR; keep that as a hard pre-B checklist item so implementers cannot revive pre-fold law.
2. Empty-brain doctor: `N` days still unspecified (pick a default band, e.g. 7d bound active, config-lower only).
3. Post-C capture default: state explicitly that unset + no provider = `distill` with fail-closed skip (not legacy regex); only explicit `regex` degrades.
4. Start-pack assembly: bound the pre-rank candidate pool (pins + salience/recency cap) so hybrid rank cannot become dump-all CPU on large stores.
5. Rehydrate partial eligibility: state that ineligible pinned rows drop and remaining keep pinned order; do not backfill replacements in P0.
6. Hermes host-contract row: if no inject surface, record `n/a — MCP side-car only` (not a silent gap beside OC/CC).
7. Trust floor enums still “align at first B PR” — acceptable, but require the enum map in the B PR description as already noted (no silent invent).

### FIRST CUT
CONFIRM: prelude RCA → B (host contract on B critical path) → C; A-min parallel with B; D parallel non-gating.  
Reject Full P0-first, A2-in-P0, turn-default, LME-S as SOTA-ready gate, and `coexist_dedup`.

### ONE-LINE SUMMARY
Freeze surface is coherent and shippable for design: Round-3 laws close the dual-bus/rehydrate/scope holes; only editorial scrub and small default/bound nits remain — no freeze-lifters.
