### VERDICT
APPROVE_WITH_NITS

### BLOCKERS
none

### NITS
1. Host-contract enablement is still a menu, not a law. P0 allow only `disable_native_inject` or `sc_only`; define each in one sentence; `coexist_dedup` is out of P0 until it has a written algorithm. `memory.inject != off` is illegal on a host until that choice is recorded. Live OpenClaw already ceded session-start to native Memory Search and shares `memories.db` with Claude Code — re-enabling a 600-token start pack while native stays on the bus is the same dual-plane failure with a smaller SC half.

2. Budget accounting is still underspecified in a 40x-adjacent way. Caps apply to the fully serialized envelope (wrapper + title + fact + metadata), not bare `fact`. `content_hash` preimage is `id + title + fact` only (exclude age/tokens/trust or compact rehydrate identity breaks). Rehydrate of pinned start-pack hashes is exempt from the 1500/2000 cumulative; clip selection by rank until every cap holds.

3. Hash ring + pinned start-pack ids must live outside the prompt, keyed by host session id. Compact/reset detection is a host-adapter requirement. If the host cannot signal compact, P0 limitation is “no rehydrate” — not silent every-turn reprint (that is turn-inject by accident and will thrash `extraSystemPromptHash`).

4. Unscoped legacy rows (missing `host_id` / `agent_id` / `project`) are not injectable. Doctor reports the excluded count. No implicit backfill to “all hosts.” Deny-by-default plus a historically shared OC/CC DB otherwise becomes either spray or a fake empty brain.

5. After C ships, default `memory.capture=distill` (fail-closed skip). `regex` only when explicitly set. If unset stays today’s regex, C never becomes the product.

6. Editorial copy-hazards before the first B PR (appendix already wins; do not implement from stale bullets): §6 pack still has `why`; §5/§13 still offer A2; §16.2 still has “RESTRICTED unless session cleared” (R2-6 forbids the hatch); §8 still gates ranker PRs on LME-S delta (D is honesty-only).

7. Empty-brain doctor needs N, a new-host grace, and a fail-reason taxonomy: empty store vs mis-scope deny vs `inject=off` vs quarantine-only junk. Otherwise operators disable doctor or green-wash.

8. A-min drift is presence / mtime / native-write events, not a content merge. Import remains A-min+. `RESTRICTED` / `quarantine` hard-exclude precedes the trust-or-pin clause. `source_ids` are opaque memory ids, not paths.

### FIRST CUT
Confirm: prelude empty-brain RCA (no feature code) → B start-only inject v2 (compact rehydrate + scope + host contract) → C fail-closed distill, with A-min parallel to B and D harness parallel non-gating.

Defer stays: A2, turn inject, Full P0, A1 projections until B+C prove value, fleet shared brain, RRF-as-product-gate, custom embedders, generic doc connectors.

If RCA shows write-path death (defence FP / hook not calling `remember` / native-only writes), insert intake fix before C. B can still ship against fixtures. That is a prelude branch, not a different cut.

### ONE-LINE SUMMARY
Freeze can lift: thesis, start-only inject, fail-closed distill, A3-leaning A-min, and non-gating D are right — fold enablement/accounting law into the first B PR, do not run another thesis round.
