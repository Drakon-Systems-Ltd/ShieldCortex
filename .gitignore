# Dependencies
node_modules/

# Build output
dist/

# Compiled output never belongs alongside the TypeScript sources. src/ holds
# only .ts (compiled output lives in dist/). A stray committed src/**/*.js is a
# second, drifting source of truth — see the divergent trust-score weights that
# accumulated in src/xray. No hand-written .js/.mjs exists under src/.
src/**/*.js

# Git worktrees (feature branches checked out alongside main)
.worktrees/

# Database files
*.db
*.db-wal
*.db-shm
!src/__fixtures__/*.db

# Environment
.env
.env.local

# IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/
.jest-localstorage*

# Local config
.mcp.json
.claude/

# Internal docs
docs/plans/
CLAUDE.md

# Blog assets (hosted externally)
assets/devto-covers/
docs/dashboard-defence.png
COMPETITIVE-ANALYSIS.md
GO-TO-MARKET.md
SAAS-ARCHITECTURE.md
PRODUCT_VISION.md
SHIELDCORTEX.md
ROADMAP.md

# Benchmark machine artefacts — SCORECARD.md is committed (linked from
# README), but the per-question report is regenerated on every run.
benchmark/longmemeval/report.json
.superpowers/
:memory:
:memory:.lock
